# Changelog

## v0.0.1 (unreleased)

- Manifest spec v0.1 (`manifest_spec.md`).
- CLI: `search`, `show`, `install`, `validate`.
- Local registry with 20 seed skills in `registry/skills.jsonl`.
- Naive trust score (placeholder until install telemetry lands).
